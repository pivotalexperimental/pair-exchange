= form_for(@project) do |f|
  = f.label(:name)
  = f.text_field(:name)
  = f.label(:owner)
  = f.text_field(:owner, type: 'email')
  = f.label(:description)
  = f.text_area(:description)
  = f.submit

